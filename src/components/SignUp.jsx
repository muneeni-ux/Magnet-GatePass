// Redundant - Centralized user creation is handled by the Add User modal in UserDetails.jsx.
// This component has been retired to avoid replication of guard/user registration screens.
export default function SignUp() {
  return null;
}
